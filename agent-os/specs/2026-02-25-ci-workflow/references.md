# References

## Existing App Code
- `app/index.js` — Express app with routes: `GET /`, `GET /health`, 404 handler
- `app/server.js` — Server bootstrap, listens on PORT or 3000
- `app/package.json` — Scripts: start, test, build (`echo 'Build complete'`)
- `app/.nvmrc` — Node version: 20
- `app/__tests__/index.test.js` — 3 existing tests (hello, health, 404)

## Project Docs
- `CLAUDE.md` — Project instructions, agent team structure, intentional deploy failure note
